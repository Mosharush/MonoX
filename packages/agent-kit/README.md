# `@monox/agent-kit`

Compiles one validated project contract into concise agent guidance. Generated files are regular files with a
source digest, so Windows checkouts and tools without symlink support cannot silently corrupt the contract.
