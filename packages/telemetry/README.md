# `@monox/telemetry`

Small vendor-neutral metric primitives with deterministic Prometheus exposition. Applications can swap the
recording sink for OpenTelemetry without changing metric names or call sites.
